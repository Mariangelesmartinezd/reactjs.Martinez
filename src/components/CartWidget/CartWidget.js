
import cart from '../img/cartshop.jpeg';

const CartWidget = () => {
    return (
        <div>
            <img src={cart} className="cart" alt="cart" />
           
        </div>
    )
};

export default CartWidget;
